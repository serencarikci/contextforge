"""Integration test fixtures.

Shared fixtures (`integration_settings`, `api_client`, `db_manager`,
`db_session`) live in the root `tests/conftest.py` so that `tests/api/` and
`tests/security/` can reuse them too.
"""

from __future__ import annotations
