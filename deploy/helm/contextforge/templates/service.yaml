apiVersion: v1
kind: Service
metadata:
  name: {{ include "contextforge.fullname" . }}
  labels:
    {{- include "contextforge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contextforge.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
